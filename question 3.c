#include <stdio.h>

int main()
{
    int u,b;
    scanf("%i",&u);
    if (u <= 100)
    {
        b = u5;
    }
    else if (u<301)
    {
        b = 500 + (u-100)8;
    }
    else
    {
        b = 2100 + (u-300)*10;
    }
    printf("total bill = %i",b);
}